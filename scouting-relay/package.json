{
  "name": "powerscout-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@upstash/redis": "^1.38.0",
    "express": "^4.21.2"
  }
}
