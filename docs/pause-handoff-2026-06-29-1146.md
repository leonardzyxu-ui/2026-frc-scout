# Pause Handoff - 2026-06-29 11:46 CST

Leo asked Codex to pause all work because he is leaving home and may lose Wi-Fi. Resume only after Leo says to continue.

## Last Safe Verified State

- The Match Scout V4 Score Signal panel was compacted so the point counter dominates the panel.
- In-app browser visual proof showed the Auto counter using about 78% of the Auto panel width and the note using about 21%.
- The stale local testing warning text `Schedule validation needs a TBA key...` was not visible on the rendered scout page.
- These checks passed after the Score Signal fix:
  - `npm run typecheck`
  - `node --test tests/adminV4UxStructure.test.mjs tests/source-hygiene.test.mjs tests/matchScoutingV4.test.mjs tests/scoutRelayPager.test.mjs`
  - `npm run build`

## In-Progress Work At Pause

Scout-facing PowerCoin betting was mid-implementation and has not been verified yet. Treat it as unfinished.

Files touched in the unfinished betting slice:

- `src/types.ts`
  - Added `PowerCoinBetSide`, `PowerCoinBetLockReason`, `PowerCoinBetSendStatus`, and `PowerCoinMatchBetSnapshot`.
  - Added optional scout-number and lock/send metadata fields to `PowerCoinBet`.
  - Added optional `scoutNumber` to `PowerCoinLedgerEntry`.
- `src/utils/matchScoutingV4.ts`
  - Added normalization for `powerCoinBet`.
- `src/utils/adminV4LocalStore.ts`
  - Bumped local DB version from 4 to 5 and added optional `scoutNumber` indexes for PowerCoin stores.
- `src/utils/scoutPowerCoins.ts`
  - New utility for building/normalizing/storing scout PowerCoin bet snapshots.
- `src/views/MatchScoutV4View.tsx`
  - Began adding the `PowerCoinBetPanel` component and imports.
  - Not yet fully wired into form state, lock-on-gameplay, local bet persistence, or submit flow.

## Resume Checklist

1. Finish wiring `PowerCoinBetPanel` into `MatchScoutV4View.tsx` before the Auto panel.
2. Implement lock on `Start Game` and first gameplay action.
3. Persist locked bet snapshots inside match JSON and as local `PowerCoinBet` rows.
4. Add tests for PowerCoin snapshot normalization and scout-side UI/source structure.
5. Run:
   - `npm run typecheck`
   - `node --test tests/adminV4UxStructure.test.mjs tests/source-hygiene.test.mjs tests/matchScoutingV4.test.mjs tests/scoutRelayPager.test.mjs`
   - `npm run build`
6. Then continue the remaining queue from `codex_task_queue.md`.

## Subagent State

- Maxwell (`019f1171-9ab0-7621-ba43-e5c8533f072f`) completed and was closed. Purpose: PowerCoin integration cartography. Model: `gpt-5.4`; reasoning: `medium`.
- Einstein (`019f10f9-ea06-7241-8f54-1cd972b344ad`) close attempt returned `not_found`; no active work remained.
- Kuhn (`019f111a-5a98-7380-a399-b3c46983cd1e`) completed and was closed. Purpose: practice-match score availability research. Model/thinking not available in compacted context.

## Goal Handling

The active goal cannot honestly be marked complete because core queued work remains unfinished. Work should remain paused until Leo explicitly resumes.
