# Cue Steward Backlog Audit 001

- Agent: Poincare / Curie task
- Agent id: `019f10c4-1a8d-7743-8199-475c51cc7ff3`
- Role: explorer / cue steward
- Model: `gpt-5.4-mini`
- Reasoning effort: medium
- Edit permission: read-only
- Status: completed

## Purpose

Audit Leo's pasted overnight prompt against `codex_task_queue.md`, `codex_swarm.md`, and the recovery note, then identify missing or too-broad queue items.

## Missing Or Underrepresented Work

- Translate the attached statistics report into explicit alliance-level math for summed means, combined standard deviations, and win probability outputs.
- Fetch and anchor the current FIRST Game Manual and Team Updates used for DPR and ranking-point logic.
- Build the mirrored strategy preview page that shows predicted opponent shift behavior and recommended response plan.
- Split first-shift disagreement handling into detection, match-scoped correction prompting, and a local/admin notification surface.
- Split browser-cache export into JSON schema and direct local-file export path.
- Add the conditional PowerScout marketing-site task only if the core scouting redesign is otherwise complete.

## Too-Broad Queue Items

- Strategy simulation should be split into alliance outcome math, RP-aware scoring, and smart-gamble selection.
- Shift UI should be split into active/inactive layout, reversible first-shift/color coding, rolling submit/no-gray history, and defender-target selection.
- Surfacing should be split by Admin V4, PowerScout, and strategy preview/recommendation views.
- SFT/QA should be split into full-event replay, shift/reconciliation/strategy tests, and failsafe checks.

## Integration Response

Added explicit queue entries and decomposition sections for rule anchors, mirrored strategy preview, export splitting, first-shift notification phases, and conditional marketing.

## Safety Notes

No files were changed by the agent. No credentials, pushes, deploys, or protected file edits were performed.

